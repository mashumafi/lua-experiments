name: main

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - uses: popperized/cmake@master
    - name: cmake
      run: cmake .
    - name: build
      run: make
    - name: test
      run: make test
